# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.venv/

# Secrets
.env

# Logs (agent logs live in logs/; stray *.log files e.g. the dashboard API)
*.log

# Node / web dashboard build artefacts
node_modules/
dist/

# Agent runtime state (main.py)
session.json
session.tmp
session.json.bak
lessons_learned.json
lessons_learned.json.tmp
logs/

# runtime state / local backups
iv_history.csv
session.json.bak*
session.json.*-retired-*
HALT
HALT.*
journal.md
journal.csv

# Frozen evidence bundle for the hosted audit dashboard — committed on purpose.
# (Overrides session.json / *.log / logs/ ignores above, for this path only.)
!audit/data/
!audit/data/session.json
!audit/data/agent.log
!audit/data/agent_activity.log
!audit/data/FINAL_SESSION_AUDIT.md
!audit/data/audit_snapshot.json
